function greaterNumber (arg1, arg2) {
    let numbA = Number(arg1);
    let numB = Number(arg2);

    if (numbA > numB){
 console.log (numbA)
    } else {
        console.log (numB)
    }
}
greaterNumber (5, 6)